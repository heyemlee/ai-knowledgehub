# Railway 部署环境变量配置模板
# 复制这些变量到 Railway 项目的 Variables 设置中

# ==========================================
# 必需配置
# ==========================================

# 运行模式
MODE=production

# OpenAI 配置
OPENAI_API_KEY=sk-your-openai-api-key-here
OPENAI_MODEL=gpt-4-turbo-preview
OPENAI_EMBEDDING_MODEL=text-embedding-3-large

# Qdrant 向量数据库配置
QDRANT_URL=https://your-cluster-id.qdrant.io
QDRANT_API_KEY=your-qdrant-api-key-here
QDRANT_COLLECTION_NAME=knowledge_base

# JWT 密钥（重要：生成强随机密钥）
# 生成命令: python3 -c "import secrets; print(secrets.token_urlsafe(64))"
JWT_SECRET_KEY=your-very-strong-random-secret-key-here
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# 数据库配置（使用 Railway 提供的变量）
DATABASE_URL=${{Postgres.DATABASE_URL}}

# 前端 URL（部署后填入实际域名）
FRONTEND_URL=https://your-frontend-domain.vercel.app

# 后端 URL（Railway 自动提供）
BACKEND_URL=${{RAILWAY_PUBLIC_DOMAIN}}

# ==========================================
# 可选配置（推荐）
# ==========================================

# Redis 缓存（强烈推荐，使用 Railway Redis）
REDIS_URL=${{Redis.REDIS_URL}}

# 日志配置
LOG_LEVEL=INFO

# ==========================================
# Railway 特有变量说明
# ==========================================

# ${{Postgres.DATABASE_URL}} - Railway PostgreSQL 数据库 URL
# ${{Redis.REDIS_URL}} - Railway Redis 连接 URL
# ${{RAILWAY_PUBLIC_DOMAIN}} - Railway 自动分配的公开域名

# ==========================================
# 安全提示
# ==========================================

# 1. 永远不要在 Git 中提交真实的密钥
# 2. JWT_SECRET_KEY 必须是强随机字符串
# 3. 定期轮换 API 密钥
# 4. 生产环境必须设置 MODE=production
# 5. 确保 FRONTEND_URL 正确配置以避免 CORS 问题

